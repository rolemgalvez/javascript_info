/* 
// debugger
function hello(name) {
  let message = `Hello ${name}`;
  debugger; // debugger stops here
  alert(message);
}

hello("Juan");
*/

// Logging
for (let i = 0; i < 5; i++) {
  console.log("value, ", i);
}
